# 贡献指南

感谢你愿意参与 TouhouFlandre。为了让项目保持清晰、可维护，所有贡献都应先通过 Issue 对齐需求、标准或数据来源，再提交 Pull Request。

## PR 前置要求

提交 PR 前，请先创建或参与一个 Issue，并对齐以下内容之一：

- 需求背景和用户场景；
- Bug 复现步骤和期望行为；
- 题库或素材的数据来源；
- 技术方案、契约变化和验收标准；
- 与[站点开发计划](./docs/site-development-plan.md)的对应关系。

未事先在 Issue 中对齐，或明显偏离站点开发计划的 PR 将被直接关闭。维护者可以要求贡献者先补充 Issue、缩小范围或拆分 PR。

## 适合贡献的内容

- 题库数据修正、来源补充和翻译改进；
- 游戏规则、搜索、分享、统计和多人房间体验改进；
- 可访问性、响应式布局和界面细节优化；
- 测试、类型、契约和工程质量改进；
- 文档、部署说明和第三方素材署名维护。

## 不接受的内容

- 未关联 Issue 的功能 PR；
- 大范围重构但没有明确收益、迁移方案或测试覆盖；
- 手工编辑生成物；
- 引入来源不明、授权不清或不允许再分发的素材；
- 将隐藏答案、guest token、房间号或对手昵称写入分享文本、本地统计导出或日志；
- 在前端重新实现权威答案选择或反馈比较；
- 将本地运行产物、缓存、数据库文件、日志或依赖目录纳入提交。

## 开发流程

1. 阅读 [README](./README.md)、[开发指南](./docs/development.md) 和相关主题文档。
2. 在 Issue 中对齐需求、验收标准和影响范围。
3. 从最新主分支创建功能分支。
4. 保持 PR 聚焦于单一主题。
5. 同步更新文档、测试和生成物。
6. 在 PR 描述中说明验证命令和未覆盖风险。

## 验证建议

常规改动至少运行：

```bash
pnpm typecheck
pnpm test
cd apps/api && go test ./...
```

涉及 API 契约或数据库查询时运行：

```bash
task gen
task check:generated
```

涉及核心交互、多人房间或响应式布局时，在 `task dev` 运行中执行：

```bash
pnpm --filter @touhouflandre/web test:e2e
```

## 题库与素材

题库贡献需遵守[数据规范](./docs/data-guidelines.md)，并在 Issue 或 PR 中列出资料来源。第三方素材贡献需同步更新 [THIRD_PARTY_ASSETS.md](./THIRD_PARTY_ASSETS.md)，说明作者、来源、许可证、仓库位置和站内用途。

## 提交信息

推荐使用 conventional commits，例如：

- `feat(web): 改进角色搜索视图`
- `fix(api): 修正重复猜测错误映射`
- `docs: 更新部署指南`
- `data: 补充角色来源`

一个提交应聚焦于单一问题，避免夹带格式化无关文件。
